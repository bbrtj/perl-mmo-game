% layout 'default';
% title _tt 'Register';

% my $gen_errors = $form->errors_hash->{''};

% if ($form->is_validated && $form->valid) {
	<h1>
		%= _tt 'Registration successful';
	</h1>

	<p>
		%= _tt 'You may now';
		%= link_to _tt('Log in') => url_for '/user/login';
	</p>
% }
% else {
	<h1>
		%= _tt 'Register'
	</h1>

	<form method='POST' action=''>
		<div class="row g-3">

			% for my $field ($form->form_html->@*) {
				%== $field->{label}
				<div class="col-sm-12">
					%== $field->{input}
				</div>
				% if ($field->{errors}) {
					<div id="register-feedback" class="form-feedback">
						%== $field->{errors}
					</div>
				% }
			% }

			% if (defined $gen_errors) {
				<div id="register-general-feedback" class="col-sm-12 form-feedback">
					% for my $error ($gen_errors->@*) {
						<p><%= $error %></p>
					% }
				</div>
			% }

			<div class="col-sm-12">
				<input class="form-control" type="submit" value="<%= _tt('Send') %>">
			</div>
		</div>
	</form>
% }
