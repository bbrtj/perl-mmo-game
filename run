#!/usr/bin/env perl

use v5.42;

my ($arg) = @ARGV;
$arg //= '';

if ($arg eq 'lint') {
	system('tidyall -a --mode lint');
}
elsif ($arg =~ /^critic/) {
	system('tidyall -a --mode ' . $arg);
}
elsif ($arg eq 'web') {
	# TODO
}
else {
	system('script/worker &');
	system('script/server');
}

