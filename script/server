#!/usr/bin/env perl

use Mojolicious::Lite;
use lib 'lib';

use Utils;
use Server::Worker;
use DI;

DI->get('log')->filename('logs/minion.log');
Utils->bootstrap(app);

plugin 'Minion::Admin';

Server::Worker->register(app->minion);

app->start;
