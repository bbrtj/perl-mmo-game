#!/usr/bin/env perl

use Mojolicious::Lite -signatures;
use Game::Common;
use Game::Worker;

Game::Common->bootstrap(app, '../vars.conf');

plugin 'Minion::Admin';

Game::Worker->register(app->minion);

app->start;
